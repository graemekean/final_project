<%= simple_form_for(@stem) do |f| %>
  <% if @stem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stem.errors.count, "error") %> prohibited this stem from being saved:</h2>

      <ul>
      <% @stem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
  
  <%= f.hidden_field :user_id, :value => current_user.id %> 

  </div>
  
  <div class="field">
    <%= f.input :description %><br>
  </div>
  <div class="field">
    <%= f.input :bpm %><br>
  </div>
  <div class="field">
    <%= f.input :key %><br>
  </div> 
  <div class="field">
    <%= f.input :instrument %><br>
  </div>
  
  <div class="field">
    <%#= f.input :project_id, collection: Project.all, label_method: :title %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>





